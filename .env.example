# 32-byte AES master key for encrypting provider secrets (API keys,
# passwords) at rest. Generate with:
#   openssl rand -base64 32
# If unset, secrets are stored in cleartext (with a boot-time warning).
MASTER_KEY=

# Host port the app listens on. Defaults to 8080.
# HOST_PORT=8080
