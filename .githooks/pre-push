#!/usr/bin/env bash
# Runs the same checks CI runs. Blocks the push on any failure.
# Skip in a pinch with `git push --no-verify`, but you'll probably
# regret it.
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"
exec make verify
